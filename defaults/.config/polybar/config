;=====================================================
;
;   To learn more about how to configure Polybar
;   go to https://github.com/jaagr/polybar
;
;   The README contains alot of information
;
;=====================================================
;TODO.RM-Sun Apr 02 2017 15:20:
;  [X] - Symbols for ws
;  [X] - Add the missing scripts from i3 bar
;	 	 [ ]- Add color for the added script
;  [X] - Fix system tray icons
; Most of scripts obtained from:
; - https://github.com/x70b1/polybar-scripts.git

include-file = ~/.config/polybar/colors.ini
include-file = ~/.config/polybar/modules.ini
include-file = ~/.config/polybar/legacy-bars.ini

[global/wm]
margin-top = 0
margin-bottom = 0

[settings]
; The throttle settings lets the eventloop swallow up til X events
; if they happen within Y millisecond after first event was received.
; This is done to prevent flood of update event.
;
; For example if 5 modules emit an update event at the same time, we really
; just care about the last one. But if we wait too long for events to swallow
; the bar would appear sluggish so we continue if timeout
; expires or limit is reached.
throttle-output = 5
throttle-output-for = 10

; Time in milliseconds that the input handler will wait between processing events
throttle-input-for = 30

; Reload upon receiving XCB_RANDR_SCREEN_CHANGE_NOTIFY events
screenchange-reload = false

; Compositing operators
; @see: https://www.cairographics.org/manual/cairo-cairo-t.html#cairo-operator-t
compositing-background = source
compositing-foreground = over
compositing-overline = over
compositing-underline = over
compositing-border = over

; Define fallback values used by all module formats
format-foreground = 
format-background = 
format-underline =
format-overline =
format-spacing =
format-padding =
format-margin =
format-offset =

; Enables pseudo-transparency for the bar
; If set to true the bar can be transparent without a compositor.
pseudo-transparency = false

[bar/main]
width = 100%
height = 2%
; offset-x = 1%
; offset-y = 1%
radius = 6.0
fixed-center = true

background = ${colors.background}
foreground = ${colors.foreground}
; foreground = ${colors.t1}
; background = ${colors.background_light}
underline-color = #00f
overline-color = #f00

line-size = 3
line-color = #f00

border-size = 4
border-color = #00000000

padding-left = 0
padding-right = 0

module-margin-left = 1
module-margin-right = 1

; override-redirect = true
enable-ipc = true

scroll-up = i3wm-wsnext
scroll-down = i3wm-wsprev


[bar/main-top]
inherit = bar/main
modules-left = i3
modules-center = xwindow
modules-right = weather date time powermenu

[bar/main-bottom]
bottom = true
width = 100%
height = 2%
; offset-x = 1%
; offset-y = 1%
radius = 6.0
fixed-center = true

background = ${colors.background}
foreground = ${colors.foreground}
; background = ${color.bground}
; foreground = ${color.fground}
underline-color = #00f
overline-color = #f00

line-size = 3
line-color = #f00

border-size = 4
border-color = #00000000

padding-left = 0
padding-right = 0

module-margin-left = 1
module-margin-right = 1
; override-redirect = true
enable-ipc = true

scroll-up = i3wm-wsnext
scroll-down = i3wm-wsprev

; My new settings
tray-detached = false
tray-position = right
tray-offset-x = 0
tray-offset-y = 0

tray-padding = 2
tray-scale = 1

[bar/bottom-predator]
inherit = bar/main-bottom
monitor = DP-0
font-0 = "Font Awesome 5 Free:style=Regular:pixelsize=18;1"
font-1 = "Font Awesome 5 Free:style=Solid:pixelsize=18;1"
font-2 = "Font Awesome 5 Brands:pixelsize=18;1"
font-3 = Roboto:size=18;0
; font-1 = unifont:fontformat=truetype:size=8:antialias=false;0
; Fri Sep 15 2017 09:18: In order to get siji font to work use this command to get the exact name:
; echo "$(fc-list : family | grep Siji | tail -1):pixelsize=10"
; If that doesnt return anything try with grep siji instead of grep Siji
; For more info look [here](https://github.com/jaagr/polybar/issues/313)

modules-left = player-cmus
modules-center = cpu system-nvidia-smi cpuload system-uptime-pretty memory wlan-predator
modules-right = battery temperature xkeyboard github updates-arch-combined redshift compton volume

tray-maxsize = 40

[bar/bottom-helios]
inherit = bar/main-bottom
font-0 = "Font Awesome 5 Free:style=Regular:pixelsize=10;1"
font-1 = "Font Awesome 5 Free:style=Solid:pixelsize=10;1"
font-2 = "Font Awesome 5 Brands:pixelsize=10;1"
font-3 = Roboto:size=10;0
; font-1 = unifont:fontformat=truetype:size=8:antialias=false;0
; Fri Sep 15 2017 09:18: In order to get siji font to work use this command to get the exact name:
; echo "$(fc-list : family | grep Siji | tail -1):pixelsize=10"
; If that doesnt return anything try with grep siji instead of grep Siji
; For more info look [here](https://github.com/jaagr/polybar/issues/313)
font-4=WunconSiji:pixelsize=10;0

modules-left = battery_helios temperature cpu system-nvidia-smi cpuload system-uptime-pretty memory wlan-down-helios wlan-up-helios
modules-center = player-cmus
modules-right = xkeyboard backlight-acpi updates-arch-combined redshift compton 

tray-maxsize = 20

[bar/top-predator]
inherit = bar/main-top
monitor = DP-0
font-0 = "Font Awesome 5 Free:style=Regular:pixelsize=18;1"
font-1 = "Font Awesome 5 Free:style=Solid:pixelsize=18;1"
font-2 = "Font Awesome 5 Brands:pixelsize=18;1"
font-3 = Roboto:size=18;0

[bar/top-helios]
font-0 = "Font Awesome 5 Free:style=Regular:pixelsize=10;1"
font-1 = "Font Awesome 5 Free:style=Solid:pixelsize=10;1"
font-2 = "Font Awesome 5 Brands:pixelsize=10;1"
font-3 = Roboto:size=10;0
; font-1 = unifont:fontformat=truetype:size=8:antialias=false;0
; Fri Sep 15 2017 09:18: In order to get siji font to work use this command to get the exact name:
; echo "$(fc-list : family | grep Siji | tail -1):pixelsize=10"
; If that doesnt return anything try with grep siji instead of grep Siji
; For more info look [here](https://github.com/jaagr/polybar/issues/313)
font-4=WunconSiji:pixelsize=10;0

